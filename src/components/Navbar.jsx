function Navbar() {
  return (
    <div
      style={{
        height: "60px",
        background: "lightblue",
        display: "flex",
        alignItems: "center",
        padding: "0 20px",
        fontWeight: "bold",
        fontSize: "18px"
      }}
    >
      SocialPulse Dashboard
    </div>
  );
}

export default Navbar;